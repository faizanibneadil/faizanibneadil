import type { DocProps } from "@/types"

export async function HackathonEntity(props: DocProps<'hackathons'>) {
    const {
        entity,
        params,
        searchParams
    } = props || {}

    return null
}